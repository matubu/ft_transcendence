CREATE TABLE Testos
(
    id numeric,
    name character varying[],
    ok boolean,
    PRIMARY KEY (id)
);
CREATE TABLE Testos4242
(
    id numeric,
    name character varying[],
    ok boolean,
    PRIMARY KEY (id)
);